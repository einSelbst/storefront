query AllBusinesses($size: Int, $cursor: String) {
  faunaDB_allBusinesses(_size: $size, _cursor: $cursor) {
    data {
      _id
      name
      display
      wikipedia
      country
    }
    before
    after
  }
}
